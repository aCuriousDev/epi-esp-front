/**
 * mapStorage — re-export de mapRepository.
 *
 * Tous les imports existants (`import { loadMap } from './mapStorage'`) continuent
 * de fonctionner sans modification. La logique réelle vit dans mapRepository.ts.
 */
export * from './mapRepository';
